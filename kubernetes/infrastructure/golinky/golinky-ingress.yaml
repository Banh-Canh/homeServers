apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: golinky
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: golinky
      port:
        number: 80
  tls:
    - hosts:
        - home
