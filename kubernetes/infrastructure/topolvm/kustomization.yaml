namespace: topolvm-system
resources:
  - namespace.yaml
  - helmrepo.yaml
  - helmrelease.yaml
configMapGenerator:
  - name: topolvm-values
    files:
      - values.yaml
generatorOptions:
  disableNameSuffixHash: true
